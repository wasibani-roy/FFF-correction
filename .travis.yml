language: python
python:
  - "3.6"
# command to install dependencies
install:
  - pip install -r requirements.txt
  - pip install coveralls

services:
  - postgresql
before_script:
  - psql -c 'create database order3_test_db;' -U postgres
  - export DATABASE_URL= order3_test_db
# command to run tests

script: pytest -v --cov=fast_food_app --cov-report term-missing 

after_success:
  - coveralls
